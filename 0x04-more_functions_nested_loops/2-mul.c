#include "main.h"
#include <stdio.h>
/**
 * mul - add two numbers.
 * @a: int parameters
 * @b: int parameters
 * Return: mult
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
